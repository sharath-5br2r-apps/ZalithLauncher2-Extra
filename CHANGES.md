# Build Trigger

  Triggered APK build for:
  - Fixed LegacyControlEditorActivity: routing to Compose editor, joystick/drawer support
  - Fixed startLegacyEditorActivity to use modern Compose-based editor
  - Added parseAllControls() with full joystick and drawer parsing
  - Added Add Joystick / Add Drawer menu items
  - Added new string resources for joystick editor UI
  